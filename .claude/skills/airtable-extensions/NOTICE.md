`SKILL.md` in this directory is vendored from Victoria Plummer's
[airtable-interface-extension-toolkit](https://github.com/victoriaplummer/airtable-interface-extension-toolkit),
MIT licensed (see `LICENSE`). Unmodified from the source repo as of 2026-07-25.

The same source repo's RGB color values (`frontend/colors.js`) and field-access
patterns (`frontend/fields.js`) were adapted into
`packages/tokens/src/colors.ts` and `packages/helpers/src/safeField.ts` on
2026-07-25 — see the attribution comments in those files.
